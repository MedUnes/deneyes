> ! This project is in a very early development stage

# DNeyeS
* A multi-thread asynchronous low-level DNS and HTTP site scanner built in Rust

## Usage:
```bash
cargo run
```

## Build:
```bash
cargo build
```

## Results:
```bash
cat export *.ndjs  | jq
```
